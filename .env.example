# PostgreSQL connection string (provided automatically by Railway when you add a database)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Set to production
NODE_ENV=production

# Port (Railway sets this automatically, no need to change)
PORT=5000
